---
services:
  nginx-proxy-manager:
    image: "jc21/nginx-proxy-manager:latest"
    container_name: nginx-proxy-manager
    restart: unless-stopped
    ports:
      - "${APP_PORT_PREFIX:-17}080:80"
      - "${APP_PORT_PREFIX:-17}081:81"
      - "${APP_PORT_PREFIX:-17}443:443"
    environment:
      - TZ=${TZ:-UTC}
    volumes:
      - ${APP_DIR:-/volumes/containers/nginx-proxy-manager}/data:/data
      - ${APP_DIR:-/volumes/containers/nginx-proxy-manager}/certs:/etc/letsencrypt
    networks:
      - nginx-proxy-manager

networks:
  nginx-proxy-manager:
    driver: bridge
